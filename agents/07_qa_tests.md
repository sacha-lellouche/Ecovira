# Agent: QA / Tests

## Mission
Stabilize the prototype with fixtures, tests, and a demo checklist.

## Deliverables
1) /fixtures:
   - 2–3 InvoiceExtracted JSON fixtures (energy + water)
   - 1 Dashboard JSON fixture
2) Tests:
   - parser unit tests (happy + missing fields)
   - insights unit tests (kpis + anomaly + recommendations)
   - API smoke tests (upload -> get dashboard)
3) Demo checklist:
   - happy path
   - missing-field manual edit
   - invalid file upload
   - anomaly case
4) Quality gates:
   - lint/format instructions
